intro (Rom version author etc) : we let it as it is
changelog: as it is
page1: additions:
	-Root corner (same as the current one)
	-Add kernel adiutor
	-Viper (same as the current one)
	-AdAway (same as the current one)
	-Vanced Youtube (same as the current one)

page2 (APPEAR ONLY IF VANCED IS SELECTED): black or dark vanced

page3 GoogleCamera

	-Google Camera
	"This option will paste the Google Camera to your internal storage.When the device boot please install it manually."
		-Dont do it.I dont want Google Camera "default option"
		-Yes.Do it
page4 Optimized LineageOS Debloater:
	-Mixplorer
		-Keep mixplorer (that will do nothing) "default option"
		-Remove mixplorer (delete_recursive("/system/app/Mixplorer"))
	-JDCToolbox
		-Add Magisk JDCToolbox (run the flashable from post2 at xda)
		-Add non-Magisk JDCToolbox  (run the flashable from post2 at xda)
		-No changes (that will do nothing) "default option"
		-Remove JDCToolbox if exists ("delete_recursive("/system/app/Toolbox")
	-Turbo app
		-Keep Turbo (that will do nothing) "default option"
		-Remove Turbo (delete_recursive("/system/app/Turbo"))
	-Eleven app
		-Keep Eleven (that will do nothing) "default option"
		-Remove Eleven (delete_recursive("/system/priv-app/Eleven"))
	-Bromide app
		-Keep Bromide (that will do nothing) "default option"
		-Remove Bromide (delete_recursive("/system/app/Bromide"))
	-LiveWallpaperPicker app
		-Keep LiveWallpaperPicker (that will do nothing) "default option"
		-Remove LiveWallpaperPicker (delete_recursive("/system/app/LiveWallpaperPicker"))

Rest pages remain be the same...




side:

-update the changelog
